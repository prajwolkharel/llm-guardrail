"""Imutable constants for data processing."""

from typing import List

KEYWORDS: List[str] = [
    "ignore previous", "disregard", "foreget everything", "you are now", "dan mode", "bypass", "jainbreak", "system prompt", "act as"
]

DATA_PATHS = {
    "processed": "data/processed/combined_prompts.parquet"
}
